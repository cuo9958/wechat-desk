import React, { Fragment } from 'react';
import { Button } from 'antd';

export default class extends React.PureComponent {
    render() {
        return <div id="message_setting">
            暂时不需要设置
        </div>
    }
}